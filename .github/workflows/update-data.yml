name: Update Stock Market Data (deprecated)

on:
  workflow_dispatch:

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - name: Show deprecation note
        run: |
          echo "This workflow has been superseded by 'Daily ETL-v2 and Data Commit'."
          echo "Trigger the new workflow if you need to run the ETL manually."
